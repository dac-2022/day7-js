// Look at the parameter
// there is no datatype for the input parameter
// not even let
// though we have not declared return type
// We can return,
function sum(i, j) {
  return i + j;
}

let output = sum(1, 1);
console.log(output);
