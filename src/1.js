const i = 99;
const price = 99.99;
const offerPrice = 0;
const username = "cdac-kochi";
const username1 = "";
const isActive = true;

// CONDITION :: with Boolean Variable
if (isActive) {
  console.log("User is active");
}

// Condition :: with String variable
if (username) {
  console.log("User is active");
}

if (username1) {
  console.log("User is active");
}

// CONDITION :: WITH NUMBER AS WELL
if (price) {
  console.log("User is active");
}

if (offerPrice) {
  console.log("will this get executed ? yes : no");
}
